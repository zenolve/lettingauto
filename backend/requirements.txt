fastapi==0.111.0
uvicorn[standard]==0.30.1
pydantic==2.7.1
pydantic-settings==2.3.0
# Supabase / Postgres data layer (direct connection via SUPABASE_DB_URL)
psycopg[binary,pool]==3.2.9
# Stripe payments (Checkout sessions + webhook)
stripe==12.0.0
httpx==0.27.0
python-jose[cryptography]==3.3.0
# PyJWT (distinct from python-jose) — used by the DocuSign client for the RS256
# JWT bearer assertion. Was only in the local venv; declare it explicitly.
PyJWT==2.8.0
passlib[bcrypt]==1.7.4
# passlib 1.7.4 is incompatible with bcrypt >= 4.1 (its backend-detection crashes
# at startup: "password cannot be longer than 72 bytes"). Pin the last good one.
bcrypt==4.0.1
python-multipart==0.0.9
aiosmtplib==3.0.1
jinja2==3.1.4
weasyprint==62.3
# WeasyPrint 62.3 only declares pydyf>=0.10.0 (no upper bound), so pip pulls
# the latest pydyf, which dropped the Stream.transform() that 62.3 calls via
# super() -> "'super' object has no attribute 'transform'" at render time.
# Pin to the floor that matches WeasyPrint 62.3.
pydyf==0.10.0
fpdf2==2.7.9
playwright==1.45.0
mammoth==1.7.1
beautifulsoup4==4.12.3
python-dateutil==2.9.0
itsdangerous==2.2.0
pytest==8.2.2
pytest-asyncio==0.23.7
